spring:
  application:
    name: realtime-leaderboard-system
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/leaderboarddb}
      auto-index-creation: true

server:
  port: ${PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
